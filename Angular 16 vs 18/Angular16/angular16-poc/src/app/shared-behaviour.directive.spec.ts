import { SharedBehaviourDirective } from './shared-behaviour.directive';

describe('SharedBehaviourDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedBehaviourDirective();
    expect(directive).toBeTruthy();
  });
});
